{% extends 'base.html.twig' %}

{% block title %}Consulter Eleve{% endblock %}

{% block contenu %}
     
    <div class="card" style="margin:15px;">
        <div class="card-header">
            <H1>Consulter élève</H1>
            <H1 style="text-align:center;">Consulter élève</H1>
        </div>
        <div class="card-body">
            <table class="table">
                <thead>
                    <tr>
                        <th>nom</th>
                        <th>prenom</th>
                        <th>classe</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                {% for eleve in eleves %}
                    <tr>
                        <td>{{ eleve.nom }}</td>
                        <td>{{ eleve.prenom }}</td>
                        <td>{{ eleve.classe }}</td>
                        <td>
                            <a href="{{ path('mairie_show', {'id': mairie.id}) }}">Consulter</a>
                            <a href="{{ path('ConsulterMairieId', {'id': mairie.id}) }}">Consulter</a>                            
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="8">Pas de valeurs trouvées</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
                    <a href="{{ path('GestionSubventions') }}">Retour</a>
                {% endfor %}
                </tbody>
            </table>
            <a href="{{ path('GestionSubventions') }}">Retour</a>
        </div>
    </div>            
{% endblock %}