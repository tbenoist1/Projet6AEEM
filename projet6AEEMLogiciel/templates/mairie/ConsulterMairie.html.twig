{% extends 'base.html.twig' %}

{% block title %}Consulter Mairie{% endblock %}

{% block contenu %}
     
    <div class="card" style="margin:15px;">
        <div class="card-header">
            <H1 style="text-align:center;">Consulter mairie</H1>
        </div>
        <div class="card-body">
            <table class="table">
                <thead>
                    <tr>
                        <th>Ville</th>
                        <th>Zone</th>
                        <th>LienDossier</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                {% for mairie in mairies %}
                    <tr>
                        <td>{{ mairie.ville }}</td>
                        <td>{{ mairie.zone }}</td>
                        <td>{{ mairie.lienDossier }}</td>
                        <td>
                            <a href="{{ path('ConsulterMairieId', {'id': mairie.id}) }}">Consulter</a>
                            
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="8">Pas de valeurs trouvées</td>
                    </tr>

                    <a href="{{ path('GestionSubventions') }}">Retour</a>
                {% endfor %}
                </tbody>
            </table>
            <a href="{{ path('GestionSubventions') }}">Retour</a>
        </div>
    </div>            
{% endblock %}